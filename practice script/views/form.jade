doctype html
html
 head
   meta(charset='utf-8')
 body
  form(action='/form_receiver' method='post') 
    P
     input(type='text' name ='title')
    P
     textarea(name="description")
    P
     input(type='submit')

//- form 의 기본 method는 get이다. 생략하면 기본 get이다
//- 현재는 Post를 받을 방법이 없다.
